[build]
  command = "yarn build"
  publish = ".next"

# If you need to use the plugin explicitly (optional – Netlify auto-detects Next.js)
[[plugins]]
  package = "@netlify/plugin-nextjs"

  [plugins.inputs]
    # Uncomment if you need custom settings
    # trailingSlash = false